<!DOCTYPE html>
<html lang="ko">

<head>
  <meta charset="UTF-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>영화 티켓 예약</title>
  <!-- 필요한 경우 추가적인 스타일이나 스크립트를 포함하세요. -->
</head>

<body>
  <button onclick="sendOrderData()">예매</button>

  <script>
    // 비동기 요청을 수행하는 함수
    function sendOrderData() {
      // 예약 데이터 가져오기
      var data = {
        "movie_time_id": 7,
        "seat_name_list": [{
            "seat_row": "C",
            "seat_col": 3
          },
          {
            "seat_row": "C",
            "seat_col": 4
          }
        ],
        "person_type": {
          "teenager": 0,
          "adult": 2,
          "senior": 0
        }
      };

      // fetch를 사용하여 AJAX 요청 수행
      fetch('http://localhost:8080/api/payment/ticket', {
          method: 'POST',
          headers: {
            'Content-Type': 'application/json'
          },
          body: JSON.stringify(data)
        })
        .then(function(response) {
          // 응답 처리
          if (!response.ok) {
            throw new Error('네트워크 응답이 실패하였습니다.');
          }
          alert('예약이 성공적으로 완료되었습니다.');
        })
        .catch(function(error) {
          // 오류 처리
          console.error('오류 발생:', error.message);
        });
    }
  </script>

</body>

</html>
<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>donguuk theater</title>
  </head>
  <body>
    <h3>줄거리</h3>
    <img src="<%= movie[0].poster_image %>" width="200" alt="포스터" />
    <h4><%= movie[0].title %></h4>
    <p><%= movie[0].summary %></p>
    <div><%= movie[0].running_time %>분</div>
    <div><%= movie[0].age_limit %>세</div>
    <div>개봉일자: <%= movie[0].released_date %></div>
  </body>
</html>
